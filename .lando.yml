name: restaurant
recipe: wordpress
config:
  php: '7.4'
  composer_version: '2.0.7'
  via: apache:2.4
  webroot: .
  database: mariadb
  xdebug: false
services:
  database:
    type: mariadb:10.6
    creds:
      database: wordpress
      user: wordpress
      password: wordpress
